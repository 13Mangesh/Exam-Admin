export class Student {
  constructor(
    public name: string,
    public rollNumber: string,
    public year: string,
    public lastTestName: string,
    public lastTestScore: number,
    public lastTestFeedback: string
  ) {}
}
